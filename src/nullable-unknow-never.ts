const searchName = (value: string | null) => {
   if(value === null) {
    console.log('No name found')
   }
}

const checkMyCarSpeed = (speed: unknown) => {
    if(speed === undefined) {
        console.log('No speed found')
    }
}